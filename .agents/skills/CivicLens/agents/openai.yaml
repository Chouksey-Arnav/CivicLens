interface:
  display_name: "CivicLens"
  short_description: "Repo-specific patterns and workflows for CivicLens"
  default_prompt: "Use the CivicLens repo skill to follow existing architecture, testing, and workflow conventions."
policy:
  allow_implicit_invocation: true